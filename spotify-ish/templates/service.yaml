apiVersion: v1
kind: Service
metadata:
  name: {{ include "spotify-ish.fullname" . }}
  labels:
    {{- include "spotify-ish.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spotify-ish.selectorLabels" . | nindent 4 }}

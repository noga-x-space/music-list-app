apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spotify-ish.fullname" . }}-test-connection"
  labels:
    {{- include "spotify-ish.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spotify-ish.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
